package providers

import (
	"github.com/gin-gonic/gin"
)

// 未完，待补充
func DB(e *gin.Engine) {
	// db, err := gorm.Open(sqlite.Open("databases/gin.db"), &gorm.Config{})

	// if err != nil {
	// 	panic("database connect error")
	// }
}
